---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: discoveries.oauth.dex.crossplane.io
spec:
  group: oauth.dex.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dex
    kind: Discovery
    listKind: DiscoveryList
    plural: discoveries
    singular: discovery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.atProvider.issuer
      name: ISSUER
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Discovery is an observe-only resource that fetches OIDC discovery
          information from Dex.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A DiscoverySpec defines the desired state of a Discovery
              resource.
            properties:
              forProvider:
                description: |-
                  DiscoveryParameters are the configurable fields of a Discovery resource.
                  Since Discovery is observe-only, there are no configurable parameters.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A DiscoveryStatus represents the observed state of a Discovery
              resource.
            properties:
              atProvider:
                description: DiscoveryObservation contains the OIDC discovery information
                  from Dex.
                properties:
                  authorizationEndpoint:
                    description: AuthorizationEndpoint is the URL of the authorization
                      endpoint.
                    type: string
                  claimsSupported:
                    description: ClaimsSupported is the list of supported claims.
                    items:
                      type: string
                    type: array
                  codeChallengeMethodsSupported:
                    description: CodeChallengeMethodsSupported is the list of supported
                      PKCE code challenge methods.
                    items:
                      type: string
                    type: array
                  deviceAuthorizationEndpoint:
                    description: DeviceAuthorizationEndpoint is the URL of the device
                      authorization endpoint.
                    type: string
                  grantTypesSupported:
                    description: GrantTypesSupported is the list of supported grant
                      types.
                    items:
                      type: string
                    type: array
                  idTokenSigningAlgValuesSupported:
                    description: IDTokenSigningAlgValuesSupported is the list of supported
                      ID token signing algorithms.
                    items:
                      type: string
                    type: array
                  introspectionEndpoint:
                    description: IntrospectionEndpoint is the URL of the token introspection
                      endpoint.
                    type: string
                  issuer:
                    description: Issuer is the OIDC issuer URL.
                    type: string
                  jwksUri:
                    description: JWKSURI is the URL of the JSON Web Key Set.
                    type: string
                  responseTypesSupported:
                    description: ResponseTypesSupported is the list of supported response
                      types.
                    items:
                      type: string
                    type: array
                  scopesSupported:
                    description: ScopesSupported is the list of supported scopes.
                    items:
                      type: string
                    type: array
                  subjectTypesSupported:
                    description: SubjectTypesSupported is the list of supported subject
                      types.
                    items:
                      type: string
                    type: array
                  tokenEndpoint:
                    description: TokenEndpoint is the URL of the token endpoint.
                    type: string
                  tokenEndpointAuthMethodsSupported:
                    description: TokenEndpointAuthMethodsSupported is the list of
                      supported token endpoint auth methods.
                    items:
                      type: string
                    type: array
                  userinfoEndpoint:
                    description: UserinfoEndpoint is the URL of the userinfo endpoint.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
